"""This is the basic module to call if the UI should be run."""

import user_interface as ui

def main():
    """Running the main loop, which starts FLRW-Net's user interface."""
    
    ui.run_user_interface()

if __name__ == "__main__":
    main()
